# MongoDB Connection
# For local development: mongodb://localhost:27017/podcast-manager
# For production: use MongoDB Atlas connection string
# Example: mongodb+srv://username:password@cluster.mongodb.net/podcast-manager
MONGODB_URI=mongodb://localhost:27017/podcast-manager

# Server Configuration
PORT=5000
NODE_ENV=development
# For production with single server: set CLIENT_URL to same domain (e.g., https://yourdomain.com)
CLIENT_URL=http://localhost:3000

# Authentication - Google OAuth 2.0
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Add authorized redirect URIs:
#    - http://localhost:5000/api/auth/google/callback (for user login)
#    - http://localhost:3000/settings (for Drive authorization)
# 3. Copy the Client ID and Client Secret here
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback
GOOGLE_DRIVE_CALLBACK_URL=http://localhost:3000/settings

# Session Secret (generate a random string - use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your-session-secret-here-minimum-32-characters

# Encryption Key (32 bytes in hex format - for Drive credentials)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-encryption-key-here-64-hex-characters

# Google Drive API (Optional - for cloud storage)
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
GOOGLE_DRIVE_FOLDER_ID=

# Download Settings
MAX_CONCURRENT_DOWNLOADS=3
CHECK_INTERVAL_HOURS=6
MAX_EPISODES_PER_CHECK=5
KEEP_EPISODE_COUNT=10

# Note: Episodes are streamed directly to Google Drive (no local storage)
